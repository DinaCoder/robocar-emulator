#ifndef MYUTILITIES_H
#define MYUTILITIES_H

#include <vector>
#include <string>

namespace processes {
    void start_smart_city (const std::vector<std::string> &v);
    void start_traffic (const std::vector<std::string> &v);
    void start_samplemyshmclient (const std::vector<std::string> &v);
    void start_map_gui (const std::vector<std::string> &v);
}

#endif // MYUTILITIES_H
